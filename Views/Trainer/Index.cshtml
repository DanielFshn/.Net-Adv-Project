@model IEnumerable<Course_Store.Models.Trainer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Skills)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YearOfExperience)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Skills)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YearOfExperience)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TrainderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TrainderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TrainderId })
        </td>
    </tr>
}

</table>
