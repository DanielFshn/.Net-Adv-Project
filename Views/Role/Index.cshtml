@model IEnumerable<Course_Store.Models.Responses.RoleViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<br />
<br />
<br />
<p>
    @Html.ActionLink("Create New", "Create",null, new { @class = "text-dark" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit","Role", new { id=item.Id },new { @class="text-dark"}) |
            @Html.ActionLink("Details", "Details","Role", new { id=item.Id },new { @class = "text-dark" }) |
            @Html.ActionLink("Delete", "Delete", "Role",new { id=item.Id },new { @class = "text-dark" })
        </td>
    </tr>
}

</table>
