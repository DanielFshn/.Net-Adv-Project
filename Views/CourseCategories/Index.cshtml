@model IEnumerable<Course_Store.Models.CourseCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<br />
<br />
<br />
<br />
<p>
    @Html.ActionLink("Create New", "Create",null,new { @class="text-dark"})
</p>
<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategotyType)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategotyType)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", "CourseCategories",new { id=item.Id },new { @class="text-dark"}) |
                @Html.ActionLink("Details", "Details", "CourseCategories", new { id=item.Id }, new { @class = "text-dark" }) |
                @Html.ActionLink("Delete", "Delete", "CourseCategories",new { id=item.Id },new { @class = "text-dark" })
            </td>
        </tr>
    }

    </table>
